import PlTabContainer from './src/Tab-Container.vue'

/* istanbul ignore next */
PlTabContainer.install = function(Vue) {
    Vue.component(PlTabContainer.name, PlTabContainer)
}

export default PlTabContainer